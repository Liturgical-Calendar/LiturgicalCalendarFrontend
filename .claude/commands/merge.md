Merge the PR using a regular merge (not squash) and delete the remote branch.
Checkout the branch that the PR was merged against.
Pull the changes, and delete the local branch if it hasn't been already deleted when merging.
